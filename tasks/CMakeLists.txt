# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_bb2TaskLib)
ADD_LIBRARY(${CAMERA_BB2_TASKLIB_NAME} SHARED 
    ${CAMERA_BB2_TASKLIB_SOURCES})
add_dependencies(${CAMERA_BB2_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${CAMERA_BB2_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CAMERA_BB2_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_BB2_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_BB2_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_BB2_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_BB2_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_bb2)

